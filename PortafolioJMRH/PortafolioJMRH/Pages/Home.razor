@layout EmptyLayout
@page "/"
@inject HttpClient HttpClient
@inject IJSRuntime JSRuntime

<style>
    @@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');

    :root {
        --primary-blue: #2196F3;
        --light-blue: #E3F2FD;
        --dark-blue: #1976D2;
        --accent-blue: #42A5F5;
        --text-dark: #0F172A;
        --text-light: #64748B;
        --white: #FFFFFF;
        --gray-light: #F8FAFC;
        --gradient-1: linear-gradient(135deg, #2196F3 0%, #1976D2 100%);
        --gradient-2: linear-gradient(135deg, #2196F3 0%, #42A5F5 100%);
        --gradient-3: linear-gradient(135deg, #64B5F6 0%, #2196F3 100%);
        --shadow-soft: 0 4px 6px -1px rgba(33, 150, 243, 0.1), 0 2px 4px -1px rgba(33, 150, 243, 0.06);
        --shadow-medium: 0 10px 15px -3px rgba(33, 150, 243, 0.1), 0 4px 6px -2px rgba(33, 150, 243, 0.05);
        --shadow-large: 0 25px 50px -12px rgba(33, 150, 243, 0.25);
        --border-radius: 20px;
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        font-family: 'Inter', system-ui, -apple-system, sans-serif;
        line-height: 1.6;
        color: var(--text-dark);
        overflow-x: hidden;
        background: var(--white);
    }

    /* Glass morphism effect */
    .glass {
        background: rgba(255, 255, 255, 0.25);
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.18);
    }

    /* Animated background */
    .bg-animated {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: -1;
        background: linear-gradient(-45deg, #2196F3, #42A5F5, #64B5F6, #1976D2);
        background-size: 400% 400%;
        animation: gradientShift 15s ease infinite;
    }

    @@keyframes gradientShift {
        0% {
            background-position: 0% 50%;
        }

        50% {
            background-position: 100% 50%;
        }

        100% {
            background-position: 0% 50%;
        }
    }

    /* Floating elements */
    .floating-elements {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        pointer-events: none;
        z-index: -1;
    }

    .floating-element {
        position: absolute;
        width: 60px;
        height: 60px;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 50%;
        animation: float 6s ease-in-out infinite;
    }

        .floating-element:nth-child(1) {
            top: 20%;
            left: 20%;
            animation-delay: 0s;
        }

        .floating-element:nth-child(2) {
            top: 60%;
            left: 80%;
            animation-delay: 2s;
        }

        .floating-element:nth-child(3) {
            top: 80%;
            left: 40%;
            animation-delay: 4s;
        }

    @@keyframes float {
        0%, 100% {
            transform: translateY(0px) rotate(0deg);
        }

        50% {
            transform: translateY(-20px) rotate(180deg);
        }
    }

    /* Header */
    .header {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
        background: var(--gradient-1);
        color: white;
        overflow: hidden;
    }

        .header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="25" cy="25" r="1" fill="rgba(255,255,255,0.1)"/><circle cx="75" cy="75" r="1" fill="rgba(255,255,255,0.1)"/><circle cx="25" cy="75" r="1" fill="rgba(255,255,255,0.05)"/><circle cx="75" cy="25" r="1" fill="rgba(255,255,255,0.05)"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>');
            animation: backgroundMove 20s linear infinite;
        }

    @@keyframes backgroundMove {
        0% {
            transform: translate(0, 0);
        }

        100% {
            transform: translate(-100px, -100px);
        }
    }

    .header-content {
        text-align: center;
        z-index: 1;
        position: relative;
        max-width: 800px;
        padding: 0 20px;
    }

    .profile-container {
        position: relative;
        display: inline-block;
        margin-bottom: 40px;
    }

    .profile-img {
        width: 180px;
        height: 180px;
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.2);
        backdrop-filter: blur(10px);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 80px;
        margin: 0 auto;
        border: 3px solid rgba(255, 255, 255, 0.3);
        animation: profilePulse 3s ease-in-out infinite;
        position: relative;
        overflow: hidden;
    }

        .profile-img::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: conic-gradient(transparent, rgba(255, 255, 255, 0.3), transparent);
            animation: rotate 4s linear infinite;
        }

    @@keyframes profilePulse {
        0%, 100% {
            transform: scale(1);
        }

        50% {
            transform: scale(1.05);
        }
    }

    @@keyframes rotate {
        0% {
            transform: rotate(0deg);
        }

        100% {
            transform: rotate(360deg);
        }
    }

    .header h1 {
        font-size: clamp(2.5rem, 5vw, 4rem);
        font-weight: 700;
        margin-bottom: 20px;
        background: linear-gradient(135deg, #ffffff 0%, #e0e7ff 100%);
        background-clip: text;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        animation: slideInUp 1s ease-out;
    }

    .header-subtitle {
        font-size: clamp(1.2rem, 2.5vw, 1.5rem);
        font-weight: 400;
        margin-bottom: 40px;
        opacity: 0.9;
        animation: slideInUp 1s ease-out 0.2s both;
    }

    .cta-buttons {
        display: flex;
        gap: 20px;
        justify-content: center;
        flex-wrap: wrap;
        animation: slideInUp 1s ease-out 0.4s both;
    }

    .btn {
        padding: 16px 32px;
        border-radius: var(--border-radius);
        text-decoration: none;
        font-weight: 600;
        font-size: 1rem;
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
        display: inline-flex;
        align-items: center;
        gap: 10px;
    }

    .btn-primary {
        background: rgba(255, 255, 255, 0.2);
        color: white;
        border: 2px solid rgba(255, 255, 255, 0.3);
        backdrop-filter: blur(10px);
    }

        .btn-primary:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
            box-shadow: var(--shadow-large);
        }

    .btn-secondary {
        background: white;
        color: var(--primary-blue);
        border: 2px solid white;
    }

        .btn-secondary:hover {
            background: var(--light-blue);
            transform: translateY(-2px);
            box-shadow: var(--shadow-large);
        }

    .btn::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
        transition: left 0.5s;
    }

    .btn:hover::before {
        left: 100%;
    }

    @@keyframes slideInUp {
        from {
            opacity: 0;
            transform: translateY(50px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* Navigation */
    .nav {
        position: fixed;
        top: 30px;
        left: 50%;
        transform: translateX(-50%);
        z-index: 1000;
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(20px);
        border-radius: 50px;
        padding: 10px 20px;
        border: 1px solid rgba(255, 255, 255, 0.2);
        transition: all 0.3s ease;
    }

        .nav.scrolled {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            box-shadow: var(--shadow-medium);
        }

    .nav-list {
        display: flex;
        list-style: none;
        gap: 30px;
        margin: 0;
        padding: 10px 0;
    }

    .nav-link {
        text-decoration: none;
        color: white;
        font-weight: 500;
        font-size: 0.9rem;
        padding: 8px 16px;
        border-radius: 25px;
        transition: all 0.3s ease;
        position: relative;
    }

    .nav.scrolled .nav-link {
        color: var(--text-dark);
    }

    .nav-link:hover, .nav-link.active {
        background: rgba(255, 255, 255, 0.2);
        color: white;
    }

    .nav.scrolled .nav-link:hover, .nav.scrolled .nav-link.active {
        background: var(--primary-blue);
        color: white;
    }

    /* Container */
    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 20px;
    }

    /* Section */
    .section {
        padding: 120px 0;
        position: relative;
    }

        .section:nth-child(even) {
            background: var(--gray-light);
        }

    .section-title {
        text-align: center;
        margin-bottom: 80px;
    }

        .section-title h2 {
            font-size: clamp(2rem, 4vw, 3.5rem);
            font-weight: 700;
            color: var(--text-dark);
            margin-bottom: 20px;
            position: relative;
            display: inline-block;
        }

            .section-title h2::after {
                content: '';
                position: absolute;
                width: 80px;
                height: 4px;
                background: var(--gradient-1);
                bottom: -15px;
                left: 50%;
                transform: translateX(-50%);
                border-radius: 2px;
            }

        .section-title p {
            color: white;
            font-size: 1.2rem;
            max-width: 600px;
            margin: 0 auto;
        }

    /* Technologies Grid */
    .tech-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 40px;
        margin-top: 60px;
    }

    .tech-card {
        background: var(--white);
        padding: 40px 30px;
        border-radius: var(--border-radius);
        box-shadow: var(--shadow-soft);
        text-align: center;
        transition: all 0.4s cubic-bezier(0.25, 0.8, 0.25, 1);
        border: 1px solid rgba(33, 150, 243, 0.1);
        position: relative;
        overflow: hidden;
    }

        .tech-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: var(--gradient-1);
            transition: left 0.5s ease;
            opacity: 0.05;
        }

        .tech-card:hover::before {
            left: 0;
        }

        .tech-card:hover {
            transform: translateY(-15px);
            box-shadow: var(--shadow-large);
            border-color: var(--primary-blue);
        }

    .tech-icon {
        width: 80px;
        height: 80px;
        background: var(--gradient-1);
        border-radius: 20px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 30px;
        font-size: 32px;
        color: white;
        position: relative;
        z-index: 1;
    }

    .tech-card h3 {
        color: var(--text-dark);
        margin-bottom: 20px;
        font-size: 1.4rem;
        font-weight: 600;
        position: relative;
        z-index: 1;
    }

    .tech-tags {
        display: flex;
        flex-wrap: wrap;
        gap: 12px;
        justify-content: center;
        position: relative;
        z-index: 1;
    }

    .tech-tag {
        background: var(--light-blue);
        color: var(--primary-blue);
        padding: 8px 16px;
        border-radius: 20px;
        font-size: 0.85rem;
        font-weight: 500;
        border: 1px solid rgba(33, 150, 243, 0.2);
        transition: all 0.3s ease;
    }

        .tech-tag:hover {
            background: var(--primary-blue);
            color: white;
            transform: translateY(-2px);
        }

    /* Projects Sections */
    .projects-section {
        background: var(--white);
    }

        .projects-section:nth-child(odd) {
            background: var(--gray-light);
        }

    .projects-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(380px, 1fr));
        gap: 40px;
    }

    .project-card {
        background: var(--white);
        border-radius: var(--border-radius);
        overflow: hidden;
        box-shadow: var(--shadow-soft);
        transition: all 0.4s cubic-bezier(0.25, 0.8, 0.25, 1);
        position: relative;
        border: 1px solid rgba(33, 150, 243, 0.1);
    }

        .project-card:hover {
            transform: translateY(-15px) scale(1.02);
            box-shadow: var(--shadow-large);
        }

    .project-header {
        background: var(--gradient-1);
        color: white;
        padding: 40px 30px;
        text-align: center;
        position: relative;
        overflow: hidden;
    }

        .project-header::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -50%;
            width: 200%;
            height: 200%;
            background: conic-gradient(transparent, rgba(255, 255, 255, 0.1), transparent);
            animation: rotate 8s linear infinite;
        }

    .project-icon {
        font-size: 60px;
        margin-bottom: 20px;
        position: relative;
        z-index: 1;
    }

    .project-header h3 {
        font-size: 1.5rem;
        font-weight: 600;
        position: relative;
        z-index: 1;
    }

    .project-content {
        padding: 35px 30px;
    }

        .project-content p {
            color: var(--text-light);
            margin-bottom: 25px;
            line-height: 1.7;
            font-size: 1rem;
        }

    .project-tech {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        margin-bottom: 20px;
    }

    .project-tech-tag {
        background: var(--gray-light);
        color: var(--text-dark);
        padding: 6px 14px;
        border-radius: 15px;
        font-size: 0.8rem;
        border: 1px solid var(--primary-blue);
        font-weight: 500;
        transition: all 0.3s ease;
    }

        .project-tech-tag:hover {
            background: var(--primary-blue);
            color: white;
            transform: translateY(-2px);
        }

    .project-actions {
        display: flex;
        gap: 15px;
        justify-content: center;
    }

    .project-link {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 12px 24px;
        background: var(--primary-blue);
        color: white;
        text-decoration: none;
        border-radius: 25px;
        font-weight: 500;
        font-size: 0.9rem;
        transition: all 0.3s ease;
    }

        .project-link:hover {
            background: var(--dark-blue);
            transform: translateY(-2px);
            box-shadow: var(--shadow-medium);
        }

        .project-link.disabled {
            background: var(--text-light);
            color: white;
            cursor: not-allowed;
            opacity: 0.7;
        }

            .project-link.disabled:hover {
                background: var(--text-light);
                transform: none;
                box-shadow: none;
            }

    /* Contact */
    .contact-info {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 40px;
        text-align: center;
    }

    .contact-item {
        background: var(--white);
        padding: 50px 30px;
        border-radius: var(--border-radius);
        box-shadow: var(--shadow-soft);
        transition: all 0.4s cubic-bezier(0.25, 0.8, 0.25, 1);
        border: 1px solid rgba(33, 150, 243, 0.1);
        position: relative;
        overflow: hidden;
    }

        .contact-item::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: var(--gradient-3);
            transition: left 0.5s ease;
            opacity: 0.05;
        }

        .contact-item:hover::before {
            left: 0;
        }

        .contact-item:hover {
            transform: translateY(-10px);
            box-shadow: var(--shadow-large);
        }

    .contact-icon {
        width: 80px;
        height: 80px;
        background: var(--gradient-3);
        color: white;
        border-radius: 20px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 25px;
        font-size: 28px;
        position: relative;
        z-index: 1;
    }

    .contact-item h3 {
        margin-bottom: 15px;
        color: var(--text-dark);
        font-weight: 600;
        font-size: 1.3rem;
        position: relative;
        z-index: 1;
    }

    .contact-item p {
        color: var(--text-light);
        font-size: 1.1rem;
        position: relative;
        z-index: 1;
    }

    /* Footer */
    .footer {
        background: var(--text-dark);
        color: white;
        text-align: center;
        padding: 60px 0;
        position: relative;
        overflow: hidden;
    }

        .footer::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 1px;
            background: var(--gradient-1);
        }

        .footer p {
            font-size: 1.1rem;
            position: relative;
            z-index: 1;
        }

    /* Scroll animations */
    .fade-in-up {
        opacity: 0;
        transform: translateY(50px);
        transition: all 0.8s cubic-bezier(0.25, 0.8, 0.25, 1);
    }

        .fade-in-up.visible {
            opacity: 1;
            transform: translateY(0);
        }

    @@keyframes spin {
        0% {
            transform: rotate(0deg);
        }

        100% {
            transform: rotate(360deg);
        }
    }

    /* Responsive */
    @@media (max-width: 768px) {
        .nav {
            top: 20px;
            left: 20px;
            right: 20px;
            transform: none;
            width: calc(100% - 40px);
        }

        .nav-list {
            gap: 15px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .tech-grid {
            grid-template-columns: 1fr;
            gap: 30px;
        }

        .projects-grid {
            grid-template-columns: 1fr;
            gap: 30px;
        }

        .contact-info {
            grid-template-columns: 1fr;
        }

        .cta-buttons {
            flex-direction: column;
            align-items: center;
        }

        .section {
            padding: 80px 0;
        }
    }

    @@media (max-width: 480px) {
        .container {
            padding: 0 15px;
        }

        .tech-card, .project-content, .contact-item {
            padding: 30px 20px;
        }
    }
</style>
<PageTitle>Cv Juan Manuel Ramirez</PageTitle>

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi Portafolio - Desarrollador .NET</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="Style.css">
</head>
<body>
    <!-- Animated Background -->
    <div class="bg-animated"></div>

    <!-- Floating Elements -->
    <div class="floating-elements">
        <div class="floating-element"></div>
        <div class="floating-element"></div>
        <div class="floating-element"></div>
    </div>

    <!-- Navigation -->
    <nav class="nav" id="navbar">
        <ul class="nav-list">
            <li><a href="#inicio" class="nav-link active">Inicio</a></li>
            <li><a href="#tecnologias" class="nav-link">Tecnologías</a></li>
            <li><a href="#proyectos-empresariales" class="nav-link">Empresariales</a></li>
            <li><a href="#proyectos-practicos" class="nav-link">Prácticos</a></li>
            <li><a href="#contacto" class="nav-link">Contacto</a></li>
        </ul>
    </nav>

    <!-- Header -->
    <header id="inicio" class="header">
        <div class="header-content">
            <div class="profile-container">
                <div class="profile-img">👨‍💻</div>
            </div>
            <h1>Desarrollador .NET</h1>
            <p class="header-subtitle">Soy desarrollador de software enfocado en crear soluciones innovadoras y eficientes tanto en entornos web como de escritorio. 
                                       Me impulsa la curiosidad y el deseo constante de aprender, lo que me lleva a explorar y dominar nuevas tecnologías para mantenerme siempre a la vanguardia. 
                                       Disfruto enfrentar desafíos que me permitan crecer profesionalmente y aportar valor mediante software que marque la diferencia.</p>
            <div class="cta-buttons">
                <a href="#proyectos-empresariales" class="btn btn-primary">
                    <i class="fas fa-code"></i>
                    Ver Proyectos
                </a>
                <a href="#" class="btn btn-secondary" onclick="downloadCV()">
                    <i class="fas fa-download"></i>
                    Descargar CV
                </a>
            </div>
        </div>
    </header>

    <!-- Technologies Section -->
    <section id="tecnologias" class="section">
        <div class="container">
            <div class="section-title fade-in-up">
                <h2>Tecnologías</h2>
                <p>Dominio completo del ecosistema Microsoft y tecnologías web modernas para crear soluciones robustas y escalables</p>
            </div>
            <div class="tech-grid">
                <div class="tech-card fade-in-up">
                    <div class="tech-icon">🏗️</div>
                    <h3>Backend & Frameworks</h3>
                    <div class="tech-tags">
                        <span class="tech-tag">C#</span>
                        <span class="tech-tag">.NET Core</span>
                        <span class="tech-tag">.NET 5-8</span>
                        <span class="tech-tag">ASP.NET</span>
                    </div>
                </div>
                <div class="tech-card fade-in-up">
                    <div class="tech-icon">🌐</div>
                    <h3>Frontend & UI</h3>
                    <div class="tech-tags">
                        <span class="tech-tag">HTML</span>
                        <span class="tech-tag">CSS</span>
                        <span class="tech-tag">JavaScript</span>
                        <span class="tech-tag">Bootstrap</span>
                        <span class="tech-tag">MudBlazor</span>
                    </div>
                </div>
                <div class="tech-card fade-in-up">
                    <div class="tech-icon">⚡</div>
                    <h3>Blazor</h3>
                    <div class="tech-tags">
                        <span class="tech-tag">Blazor Server</span>
                        <span class="tech-tag">Blazor WebAssembly</span>
                    </div>
                </div>
                <div class="tech-card fade-in-up">
                    <div class="tech-icon">🖥️</div>
                    <h3>Desktop</h3>
                    <div class="tech-tags">
                        <span class="tech-tag">Windows Forms</span>
                    </div>
                </div>
                <div class="tech-card fade-in-up">
                    <div class="tech-icon">🗄️</div>
                    <h3>Base de Datos</h3>
                    <div class="tech-tags">
                        <span class="tech-tag">SQL Server</span>
                        <span class="tech-tag">MySQL</span>
                        <span class="tech-tag">PostgreSQL</span>
                        <span class="tech-tag">Entity Framework</span>
                        <span class="tech-tag">ADO.NET</span>
                    </div>
                </div>
                <div class="tech-card fade-in-up">
                    <div class="tech-icon">☁️</div>
                    <h3>Cloud</h3>
                    <div class="tech-tags">
                        <span class="tech-tag">Azure</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Proyectos Empresariales Section -->
    <section id="proyectos-empresariales" class="section projects-section">
        <div class="container">
            <div class="section-title fade-in-up">
                <h2>Proyectos Empresariales</h2>
                <p>Soluciones robustas y escalables diseñadas para empresas, con arquitecturas complejas y alto rendimiento</p>
            </div>
            <div class="projects-grid">
                <div class="project-card fade-in-up">
                    <div class="project-header">
                        <div class="project-icon">🏢</div>
                        <h3>Taxco Finances</h3>
                    </div>
                    <div class="project-content">
                        <p>sistema web de pago de nóminas para gestionar el procesamiento de nóminas de los trabajadores de las empresas registradas en el sistema de Taxco Finances. 
                            El sistema permite a las empresas administrar de manera eficiente sus procesos de pago, asegurando precisión y cumplimiento normativo.</p>
                        <div class="project-tech">
                            <span class="project-tech-tag">Blazor Web Assembly</span>
                            <span class="project-tech-tag">C#</span>
                            <span class="project-tech-tag">Html</span>
                            <span class="project-tech-tag">Css</span>
                            <span class="project-tech-tag">JavaScript</span>
                            <span class="project-tech-tag">Bootstrap</span>
                            <span class="project-tech-tag">.NET 7.0 - 8.0</span>
                            <span class="project-tech-tag">EntityFramework</span>
                            <span class="project-tech-tag">MudBlazor</span>
                            <span class="project-tech-tag">SyncFusion</span>
                            <span class="project-tech-tag">SQL Server</span>
                        </div>
                        <div class="project-actions">
                            <a href="#" class="project-link disabled">
                                <i class="fab fa-github"></i>
                                Privado
                            </a>
                            <a href="https://taxcopay.com/" class="project-link" target="_blank" rel="noopener">
                                <i class="fas fa-external-link-alt"></i>
                                Ver Web
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Proyectos Prácticos Section -->
    <section id="proyectos-practicos" class="section projects-section">
        <div class="container">
            <div class="section-title fade-in-up">
                <h2>Proyectos Prácticos</h2>
                <p>Desarrollos enfocados en demostrar habilidades técnicas y explorar nuevas tecnologías</p>
            </div>
            <div class="projects-grid">
                <div class="project-card fade-in-up">
                    <div class="project-header">
                        <div class="project-icon">📝</div>
                        <h3>Virtualizacion De Componentes - Formularios</h3>
                    </div>
                    <div class="project-content">
                        <p>Este proyecto tiene como objetivo digitalizar diversos formularios que tradicionalmente se llenaban a mano, 
                            transformándolos en formularios web interactivos y accesibles. Esto facilita la recolección y gestión de datos, 
                            mejorando la eficiencia y reduciendo errores manuales.</p>
                        <div class="project-tech">
                            <span class="project-tech-tag">Blazor WASM</span>
                            <span class="project-tech-tag">MudBlazor</span>
                            <span class="project-tech-tag">C#</span>
                            <span class="project-tech-tag">Html</span>
                            <span class="project-tech-tag">Css</span>
                            <span class="project-tech-tag">JavaScript</span>
                            <span class="project-tech-tag">SQL Server</span>
                            <span class="project-tech-tag">.NET 5.0 - 6.0</span>
                        </div>
                        <div class="project-actions">
                            <a href="https://github.com/JuanManuel753/ProyectoEmcartago" class="project-link" target="_blank" rel="noopener">
                                <i class="fab fa-github"></i>
                                Ver Código
                            </a>
                        </div>
                    </div>
                </div>

                <div class="project-card fade-in-up">
                    <div class="project-header">
                        <div class="project-icon">💻</div>
                        <h3>Proyecto de Entrada y Salida (Escritorio)</h3>
                    </div>
                    <div class="project-content">
                        <p>Este proyecto automatiza el proceso de entrada y salida de implementos utilizando tecnología de código de barras, 
                            mejorando la eficiencia y la precisión en la gestión de inventarios y el ingreso de personas.</p>
                        <div class="project-tech">
                            <span class="project-tech-tag">Windows Forms</span>
                            <span class="project-tech-tag">.NetCore</span>
                            <span class="project-tech-tag">Bunifu UI</span>
                        </div>
                        <div class="project-actions">
                            <a href="https://github.com/JuanManuel753/Proyecto-Entrada-y-Salida" class="project-link" target="_blank" rel="noopener">
                                <i class="fab fa-github"></i>
                                Ver Código
                            </a>
                        </div>
                    </div>
                </div>

                <div class="project-card fade-in-up">
                    <div class="project-header">
                        <div class="project-icon">🎥</div>
                        <h3>Proyecto de Carteleras de Cine</h3>
                    </div>
                    <div class="project-content">
                        <p>Este proyecto, completado como parte de un curso en Udemy, consiste en desarrollar una aplicación web para mostrar las carteleras de cine, 
                            incluyendo detalles de películas, votos a las peliculas, Muestra de actores y sus personajes.</p>
                        <div class="project-tech">
                            <span class="project-tech-tag">Blazor Web Assembly</span>
                            <span class="project-tech-tag">MudBlazor</span>
                            <span class="project-tech-tag">C#</span>
                            <span class="project-tech-tag">Html</span>
                            <span class="project-tech-tag">JavaScript</span>
                            <span class="project-tech-tag">Css</span>
                            <span class="project-tech-tag">EntityFramework</span>
                            <span class="project-tech-tag">.NET 5.0 - 6.0 - 7.0</span>
                            <span class="project-tech-tag">EntityFramework</span>
                            <span class="project-tech-tag">SQL Server</span>
                        </div>
                        <div class="project-actions">
                            <a href="https://github.com/JuanManuel753/BlazorPeliculasNet5IdentityServer4" class="project-link" target="_blank" rel="noopener">
                                <i class="fab fa-github"></i>
                                Ver Código
                            </a>
                        </div>
                    </div>
                </div>

                <div class="project-card fade-in-up">
                    <div class="project-header">
                        <div class="project-icon">🎮</div>
                        <h3>Juego de Saltos (Estilo Dino de Google Chrome)</h3>
                    </div>
                    <div class="project-content">
                        <p>Este proyecto es un juego de saltos similar al famoso juego del dinosaurio de Google Chrome, donde el jugador debe evitar obstáculos saltando sobre ellos. 
                            El juego incluirá un selector de personajes que permite al jugador elegir entre tres diferentes personajes.</p>
                        <div class="project-tech">
                            <span class="project-tech-tag">Html</span>
                            <span class="project-tech-tag">Css</span>
                            <span class="project-tech-tag">JavaScript</span>
                        </div>
                        <div class="project-actions">
                            <a href="https://github.com/tuusuario/blog-blazor" class="project-link" target="_blank" rel="noopener">
                                <i class="fab fa-github"></i>
                                Ver Código
                            </a>
                            <a href="https://juegodesaltosjuanmanuelramirez.netlify.app/index.html" class="project-link" target="_blank" rel="noopener">
                                <i class="fas fa-gamepad"></i>
                                Ver Juego
                            </a>
                        </div>
                    </div>
                </div>

                <div class="project-card fade-in-up">
                    <div class="project-header">
                        <div class="project-icon">⌚</div>
                        <h3>Reloj Analogico - Digital</h3>
                    </div>
                    <div class="project-content">
                        <p>Este proyecto consiste en la creación de un reloj que muestra la hora tanto en formato analógico como digital. 
                            La interfaz incluye un reloj con manecillas que representan las horas, minutos y segundos, así como una pantalla digital que muestra la hora en formato numérico</p>
                        <div class="project-tech">
                            <span class="project-tech-tag">Blazor</span>
                            <span class="project-tech-tag">Hmtml</span>
                            <span class="project-tech-tag">Css</span>
                            <span class="project-tech-tag">JavaScript</span>
                        </div>
                        <div class="project-actions">
                            <a href="https://github.com/tuusuario/dev-tools" class="project-link" target="_blank" rel="noopener">
                                <i class="fab fa-github"></i>
                                Ver Código
                            </a>
                            <a href="https://relojanalogodigitaljuanmanuelramiez.netlify.app/" class="project-link" target="_blank" rel="noopener">
                                <i class="fas fa-external-link-alt"></i>
                                Ver Reloj
                            </a>
                        </div>
                    </div>
                </div>

                <div class="project-card fade-in-up">
                    <div class="project-header">
                        <div class="project-icon">🖩</div>
                        <h3>Caculadora</h3>
                    </div>
                    <div class="project-content">
                        <p>Este proyecto implica desarrollar una calculadora que no solo realiza operaciones aritméticas básicas (suma, resta, multiplicación y división)
                            sino que también incluye la funcionalidad para calcular la raíz cuadrada de un número.</p>
                        <div class="project-tech">
                            <span class="project-tech-tag">Blazor</span>
                            <span class="project-tech-tag">Hmtml</span>
                            <span class="project-tech-tag">Css</span>
                            <span class="project-tech-tag">JavaScript</span>
                        </div>
                        <div class="project-actions">
                            <a href="https://github.com/tuusuario/data-visualizer" class="project-link" target="_blank" rel="noopener">
                                <i class="fab fa-github"></i>
                                Ver Código
                            </a>
                            <a href="https://calculadorajuanmanuelramirez.netlify.app/" class="project-link" target="_blank" rel="noopener">
                                <i class="fas fa-external-link-alt"></i>
                                Ver Calculadora
                            </a>
                        </div>
                    </div>
                </div>
                <div class="project-card fade-in-up">
                    <div class="project-header">
                        <div class="project-icon">🎧</div>
                        <h3>Reproductor de Música</h3>
                    </div>
                    <div class="project-content">
                        <p>
                            Este proyecto consiste en desarrollar un reproductor de música que permita a los usuarios reproducir, pausar y navegar entre pistas de audio. 
                            El reproductor ofrecerá una interfaz amigable con controles básicos y una lista de reproducción.
                        </p>
                        <div class="project-tech">
                            <span class="project-tech-tag">Blazor</span>
                            <span class="project-tech-tag">Hmtml</span>
                            <span class="project-tech-tag">Css</span>
                            <span class="project-tech-tag">JavaScript</span>
                        </div>
                        <div class="project-actions">
                            <a href="https://musicplayerjuanmanuelramirez.netlify.app/" class="project-link" target="_blank" rel="noopener">
                                <i class="fab fa-github"></i>
                                Ver Código
                            </a>
                            <a href="https://musicplayerjuanmanuelramirez.netlify.app/" class="project-link" target="_blank">
                                <i class="fas fa-external-link-alt"></i>
                                Ver Reproductor
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section id="contacto" class="section">
        <div class="container">
            <div class="section-title fade-in-up">
                <h2>Contacto</h2>
                <p>¿Listo para llevar tu proyecto al siguiente nivel? Conectemos y hagamos realidad tus ideas tecnológicas</p>
            </div>
            <div class="contact-info">
                <a href="mailto:juanmanuelramirez753@outlook.com" target="_blank">
                <div class="contact-item fade-in-up">
                    <div class="contact-icon">
                        <i class="fas fa-envelope"></i>
                    </div>
                    <h3>Correo</h3>
                </div>
                </a>
                <a href="https://wa.me/+573176374276" target="_blank">
                <div class="contact-item fade-in-up">
                    <div class="contact-icon">
                        <i class="fab fa-whatsapp"></i>
                    </div>
                    <h3>Teléfono - WhatsApp</h3>
                </div>
                </a>
                <a href="https://www.linkedin.com/in/juan-manuel-r-08885b128/" target="_blank">
                <div class="contact-item fade-in-up">
                    <div class="contact-icon">
                        <i class="fab fa-linkedin"></i>
                    </div>
                    <h3>LinkedIn</h3>
                </div>
                </a>
                <a href="https://github.com/JuanManuel753" target="_blank">
                <div class="contact-item fade-in-up">
                    <div class="contact-icon">
                        <i class="fab fa-github"></i>
                    </div>
                    <h3>GitHub</h3>
                </div>
                </a>

            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <p>&copy; 2025 Desarrollador .NET. Transformando ideas en soluciones tecnológicas innovadoras.</p>
        </div>
    </footer>

    <script>

                // Navbar scroll effect
                window.addEventListener('scroll', () => {
                    const navbar = document.getElementById('navbar');
                    if (window.scrollY > 100) {
                        navbar.classList.add('scrolled');
                    } else {
                        navbar.classList.remove('scrolled');
                    }
                });

                // Smooth scrolling with active link highlighting
                document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                    anchor.addEventListener('click', function (e) {
                        e.preventDefault();
                        const target = document.querySelector(this.getAttribute('href'));

                        // Remove active class from all links
                        document.querySelectorAll('.nav-link').forEach(link => {
                            link.classList.remove('active');
                        });

                        // Add active class to clicked link
                        this.classList.add('active');

                        target.scrollIntoView({
                            behavior: 'smooth',
                            block: 'start'
                        });
                    });
                });

                // Intersection Observer for animations
                const observerOptions = {
                    threshold: 0.1,
                    rootMargin: '0px 0px -50px 0px'
                };

                const observer = new IntersectionObserver(function(entries) {
                    entries.forEach(entry => {
                        if (entry.isIntersecting) {
                            entry.target.classList.add('visible');
                        }
                    });
                }, observerOptions);

                // Observe all fade-in elements
                document.querySelectorAll('.fade-in-up').forEach(el => {
                    observer.observe(el);
                });

                // Section highlighting in navbar
                const sections = document.querySelectorAll('section');
                const navLinks = document.querySelectorAll('.nav-link');

                const sectionObserver = new IntersectionObserver((entries) => {
                    entries.forEach(entry => {
                        if (entry.isIntersecting) {
                            const id = entry.target.getAttribute('id');
                            navLinks.forEach(link => {
                                link.classList.remove('active');
                                if (link.getAttribute('href') === `#${id}`) {
                                    link.classList.add('active');
                                }
                            });
                        }
                    });
                }, { threshold: 0.3 });

                sections.forEach(section => {
                    sectionObserver.observe(section);
                });

                // CV Download function
                        function downloadCV() {
            // Ruta al PDF en wwwroot
            const pdfUrl = "Profile.pdf";

            const a = document.createElement("a");
            a.href = pdfUrl;
            a.download = "Profile.pdf"; // Nombre del archivo al descargar
            document.body.appendChild(a);
            a.click();
            document.body.removeChild(a);

            // Notificación personalizada
            showNotification("CV descargado exitosamente", "success");
        }
                // Notification system
                function showNotification(message, type = 'info') {
                    const notification = document.createElement('div');
                    notification.className = `notification notification-${type}`;
                    notification.innerHTML = `
                        <i class="fas fa-${type === 'success' ? 'check-circle' : 'info-circle'}"></i>
                        <span>${message}</span>
                    `;

                    // Estilos para la notificación
                    notification.style.cssText = `
                        position: fixed;
                        top: 30px;
                        right: 30px;
                        background: ${type === 'success' ? '#10B981' : '#2196F3'};
                        color: white;
                        padding: 16px 24px;
                        border-radius: 12px;
                        display: flex;
                        align-items: center;
                        gap: 12px;
                        font-weight: 500;
                        z-index: 10001;
                        box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
                        transform: translateX(100%);
                        transition: transform 0.3s ease;
                    `;

                    document.body.appendChild(notification);

                    // Animar entrada
                    setTimeout(() => {
                        notification.style.transform = 'translateX(0)';
                    }, 100);

                    // Remover después de 3 segundos
                    setTimeout(() => {
                        notification.style.transform = 'translateX(100%)';
                        setTimeout(() => {
                            document.body.removeChild(notification);
                        }, 300);
                    }, 3000);
                }

                // Parallax effect for floating elements
                window.addEventListener('scroll', () => {
                    const scrolled = window.pageYOffset;
                    const parallaxElements = document.querySelectorAll('.floating-element');
                    const speed = 0.5;

                    parallaxElements.forEach((element, index) => {
                        const yPos = -(scrolled * speed * (index + 1) * 0.5);
                        element.style.transform = `translateY(${yPos}px)`;
                    });
                });

                // Tech tags hover effect
                document.querySelectorAll('.tech-tag, .project-tech-tag').forEach(tag => {
                    tag.addEventListener('mouseenter', function() {
                        this.style.transform = 'translateY(-2px) scale(1.05)';
                    });

                    tag.addEventListener('mouseleave', function() {
                        this.style.transform = 'translateY(0) scale(1)';
                    });
                });

                // Performance optimization: Throttle scroll events
                let ticking = false;

                function updateOnScroll() {
                    // Navbar effect
                    const navbar = document.getElementById('navbar');
                    if (window.scrollY > 100) {
                        navbar.classList.add('scrolled');
                    } else {
                        navbar.classList.remove('scrolled');
                    }
                    ticking = false;
                }

                window.addEventListener('scroll', () => {
                    if (!ticking) {
                        requestAnimationFrame(updateOnScroll);
                        ticking = true;
                    }
                });

                // Add Easter egg
                let clickCount = 0;
                document.querySelector('.profile-img').addEventListener('click', () => {
                    clickCount++;
                    if (clickCount === 5) {
                        showNotification('¡Has encontrado un Easter Egg! 🎉', 'success');
                        document.querySelector('.profile-img').style.animation = 'profilePulse 0.5s ease-in-out 3';
                        clickCount = 0;
                    }
                });

                // Handle project links
                document.querySelectorAll('.project-link:not(.disabled)').forEach(link => {
                    link.addEventListener('click', function(e) {
                        // Si el enlace está marcado como disabled, prevenir la navegación
                        if (this.classList.contains('disabled')) {
                            e.preventDefault();
                            showNotification('Este proyecto no está disponible públicamente', 'info');
                        }
                    });
                });

                // Initialize animations on load
                document.addEventListener('DOMContentLoaded', () => {
                    // Add staggered animation delays
                    document.querySelectorAll('.tech-card').forEach((card, index) => {
                        card.style.animationDelay = `${index * 0.1}s`;
                    });

                    document.querySelectorAll('.project-card').forEach((card, index) => {
                        card.style.animationDelay = `${index * 0.15}s`;
                    });

                    document.querySelectorAll('.contact-item').forEach((item, index) => {
                        item.style.animationDelay = `${index * 0.1}s`;
                    });
                }); </script>
</body>
</html>